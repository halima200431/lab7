<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- On passe en NoActionBar pour gérer la Toolbar dans le layout -->
    <style name="Theme.StarsGallery" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>

        <!-- Couleur du texte dans les champs de saisie (SearchView) -->
        <item name="editTextColor">@color/white</item>
        <item name="android:editTextColor">@color/white</item>
        <item name="android:textColorHint">@color/text_secondary</item>
    </style>

    <!-- Style spécifique pour la Toolbar pour forcer les icônes et le texte en blanc/accent -->
    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:editTextColor">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="colorAccent">@color/accent</item>
    </style>
</resources>